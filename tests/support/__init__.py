"""Shared test helpers for the core-hardening plan (REFACTOR_PLAN_CORE).

Kept deliberately tiny and dependency-free so the self-contained golden/contract
tests can import reusable assertions without a shared conftest.
"""
